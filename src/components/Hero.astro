---
import { Image } from 'astro:assets';
import sygnet from '../assets/sygnet.svg';
---

<section class="relative py-20 lg:py-32 overflow-hidden bg-[#000D77]">
  <!-- Content -->
  <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
      <!-- Left side - Text and buttons -->
      <div class="text-white">
        <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold mb-6 leading-tight">
          Wszystkie zadania graficzne w ramach jednej subskrypcji!
        </h1>
        <p class="text-lg md:text-xl mb-8 text-white/90 leading-relaxed">
          Wzmocnij swój zespół projektantem, tak jak by był u Ciebie w biurze za ułamek kwoty jaką byś zapłacił za jego etat!
        </p>

        <div class="flex flex-col sm:flex-row gap-4">
          <!-- Primary button -->
          <a
            href="#contact"
            class="inline-flex items-center justify-center px-8 py-4 text-base font-semibold text-white bg-[#0068FF] rounded-full hover:bg-[#0058DD] transition-all duration-300 hover:scale-105 shadow-lg hover:shadow-xl"
          >
            Przetestuj za darmo!
          </a>

          <!-- Secondary button -->
          <a
            href="#how-it-works"
            class="inline-flex items-center justify-center px-8 py-4 text-base font-semibold text-white border-2 border-[#00D624] rounded-full hover:bg-[#00D624] hover:text-[#000D77] transition-all duration-300 hover:scale-105"
          >
            Jak to działa?
          </a>
        </div>
      </div>

      <!-- Right side - Sygnet SVG -->
      <div class="flex items-center justify-center lg:justify-end">
        <Image
          src={sygnet}
          alt="CMDX Sygnet"
          class="w-full max-w-md lg:max-w-lg"
          loading="eager"
        />
      </div>
    </div>
  </div>
</section>
